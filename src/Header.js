import React from 'react';

const Header = ({Header}) => {
  return (
    <header>
      {/* Skip link */}
      <a href="#maincontent" class="skip-link" title="Skip to Main">Skip to Main</a>

      <h1> Coffee Shops </h1> <span>Where to find caffeine in Milawaukie, OR</span>
    </header>
  )
}

export default Header
